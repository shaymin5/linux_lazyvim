# 💤 LazyVim

# Step

1. 安装最新unstable版neovim
```bash
sudo add-apt-repository ppa:neovim-ppa/unstable -y
sudo apt update
sudo apt install neovim git ripgrep
```
2. 克隆lazyvim仓库
```bash
git clone https://github.com/LazyVim/starter ~/.config/nvim
```
3. 清除.git
```bash
rm -rf ~/.config/nvim/.git
```
4. 启动nvim
```bash
nvim
```
5. 克隆本仓库到
